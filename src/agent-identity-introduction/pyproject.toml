[project]
name = "google-drive-demo-mcp"
version = "0.1.0"
description = "Google Drive MCP server with Descope auth"
requires-python = ">=3.12"
dependencies = [
    "descope>=1.8.0",
    "descope-mcp>=0.1.0",
    "fastmcp>=2.0.0",
    "httpx>=0.27.0",
]
